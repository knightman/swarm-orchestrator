# ─── Swarm Cluster ───────────────────────────────────────────────────
# SSH connection to the swarm manager node (user@hostname or user@ip)
SWARM_MANAGER_SSH=user@manager-node

# ─── Docker Registry ────────────────────────────────────────────────
# Private registry address (ip:port or hostname:port)
# Used in deploy.sh, docker-compose.prod.yml, and service definitions
REGISTRY_HOST=192.168.1.100:5000

# ─── Application ─────────────────────────────────────────────────────
# Port the orchestrator app listens on (host and container)
APP_PORT=8080

# ─── Host Paths (on the manager node) ───────────────────────────────
# Directory on the manager to bind-mount as /projects in the container
PROJECTS_HOST_PATH=/home/user/projects

# ─── Backend Settings ───────────────────────────────────────────────
# Read automatically by pydantic-settings in backend/config.py
DOCKER_HOST=unix:///var/run/docker.sock
REGISTRY_URL=http://localhost:5000
DATABASE_PATH=./data/swarm_orchestrator.db
DEFINITIONS_DIR=./definitions
PROJECTS_DIR=/projects
HEALTH_CHECK_INTERVAL=30
HOST=0.0.0.0
PORT=8080

# ─── MCP Integrations (optional) ────────────────────────────────────
# Ollama host URL (if running an Ollama MCP server)
OLLAMA_HOST=http://localhost:11434
# OpenWebUI URL and API key (if running an OpenWebUI MCP server)
OPENWEBUI_URL=http://localhost:3000
OPENWEBUI_API_KEY=
